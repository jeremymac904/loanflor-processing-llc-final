# Lender sources

Use `<lender-id>/overlays/` and `<lender-id>/programs/` for metadata and rights-approved normalized rules. Never mix lender overlays into agency source records. All products require explicit scope and review. Private original documents remain outside Git.

