# Tools & MCP — Malcolm

Primary tools: scoped local file search/read, document extraction, spreadsheet/calculation tools, deterministic calculators, approved LOS/CRM read/search via Zapier MCP where configured. File writes should target staging/worksheet outputs rather than overwrite originals.

## Global constraints
- least privilege;
- local folder roots scoped to role;
- Zapier MCP actions scoped to role;
- external side effects use the Ashley Approval Center in Assisted mode;
- no generic arbitrary API action unless reviewed;
- no permanent deletes;
- no credential disclosure.
